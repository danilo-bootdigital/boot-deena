// Este arquivo será gerado automaticamente pelo comando:
// pnpm --filter @agente-ia/database generate-types
//
// Por enquanto, placeholder vazio até o Supabase estar rodando.
export type Database = Record<string, never>;
